hbase的概述：（是什么、简单理解）
hbase数据库优点：
hbase的特点：
hbase数据库特点：（支持、底层存储格式、擅长存储）
hbase的一条数据组成与各部分的意义：
如何进入hbase：
定位一个单元格需要几步、分别是：
--------------------系统架构-----------------------------
类别、操作系统、文件系统、数据库是什么：
HDFS为hbase提供什么支持：
如何理解region：
如何理解列簇、对应关系是什么（在region中、在hdfs中）：
hbase的一张表中的数据是如何排序的：
hbase的系统架构由什么组成、各部分负责什么：（master、regionserver、zookeeper、hdfs、client）
hbase的region是的是怎么来的：
store的组成（由什么组成、内部组成）：
region的定位问题（如何找到某个rowkey所在的region）：
    说明：
    root region：
    .META.表：
hbase的启动命令：
hbase的web页面：
--------------------面试问题----------------------------
rowkey的设计原则：
hbase导入数据的几种方式：
为什么由列簇的概念；
列簇的设计原则：
hbase与hive的对比：
hbase存储大量的小文件的方案：
如果每天的数据增量比较大，设计表有哪些建议：
    rowkey方面：
    参数方面：
什么是布隆过滤器：
如何快速上传大量的数据到hdfs上：